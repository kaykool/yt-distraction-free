{
  "name": "yt-distraction-free",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the YT Distraction Free extension (the shipped extension itself has zero dependencies).",
  "scripts": {
    "test": "bun test test/",
    "test:blink": "bun test ./bench/blink-parity.js",
    "bench": "bun run bench/bench.js",
    "bench:update": "bun run bench/bench.js --write",
    "verify:dom": "bun run bench/verify-dom.js",
    "verify:css": "bun run bench/verify-css.js",
    "verify": "bun test test/ && bun run test:blink && bun run verify:dom && bun run verify:css"
  }
}
